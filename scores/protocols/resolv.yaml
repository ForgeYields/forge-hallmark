# yaml-language-server: $schema=../../schemas/protocol.schema.json
name: Resolv
slug: resolv
chains:
  - ethereum
  - bnb
  - plasma
category: synthetic-dollar-plus-insurance-token
assessment:
  date: '2026-05-22'
  methodology_version: v4.3
prs: 7.53
criteria:
  c1: 5
  c2: 10
  c3: 9
  c4: 9.4
  c5: 8
  c6: 4
# - Tier B+ NOT eligible: privately held DeFi issuer, no SEC reporting, no SOC on key custody,
#   no banking regulator, no public custody disclosure. v4.3 §2.3 explicitly excludes DeFi.
# - C3 = 9 (v3.9 Tier A): AWS-KMS single-key on SERVICE_ROLE drain-vector (mint + price-setter).
#   Resolv postmortem explicitly declines to disclose remediation specifics → strict reading
#   default = 9. No Tier B (MPC attestation) or Tier C (on-chain Timelock + Safe) invoked.
#   > $50M (Resolv $34M direct + Fluid $19.3M+ + Morpho ~$6M); compensation partial; no
#   remediation plan ~2 months post-exploit (Gauntlet on record); most operations still paused.
# - PRS 7.53 > 7.0 (quadruple trigger).
# - TVL: $30.1M (−47.9% over 30d; 4.4% of ATH); further leg-down on 2026-05-12 ($58M → $34M).
