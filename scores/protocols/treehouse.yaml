# yaml-language-server: $schema=../../schemas/protocol.schema.json
name: Treehouse
slug: treehouse
chains:
  - ethereum
category: yield-aggregator
assessment:
  date: '2026-05-22'
  methodology_version: v4.3
prs: 5.08
criteria:
  c1: 7
  c2: 10
  c3: 4
  c4: 2.9
  c5: 5
  c6: 2
# v4.3 re-assessment (supersedes v3.1 from 2026-04-10):
# - C1 = 7 unchanged: v3.6 audit-coverage-gap floor — two ToB Aug 2024 High findings
#   ("Accounting logic", "Reliance on Privileged Actors") remain Acknowledged-not-fixed
#   since Aug 2024 (~21 months > 90d threshold). 13+ audits across ToB×4, Sigma Prime×3,
#   WatchPug×6, Fuzzland — most recent WatchPug Dec 2025. Bounty $250K (< $1M top-band).
# - C2 = 10 (was 4 in v3.1): TVL collapsed $418M → $87.46M (DefiLlama 2026-05-27).
#   Base 8 ($87M < $100M) + 30d drawdown -22.1% (+1) + current/ATH 14.3% (+1) = 10.
#   ATH was $613.5M on 2025-08-14; current = 14% of ATH.
# - C3 = 4 (was 6 in v3.1): On-chain verification corrects v3.1 misattribution.
#   The v3.1 assessment listed Unlock Protocol's TimelockController/ProxyAdmin
#   as Treehouse's. Actual topology verified 2026-05-22 on Ethereum mainnet:
#     - Treehouse Timelock 0x2225DAbFfC7F862c99477381E971E8B1FDaB467e
#       (getMinDelay = 432000s = 5 days)
#     - Governance Safe 0x22261B4D6F629D8cF946C3524df86bF7222901F6
#       (getThreshold=5, 7 owners → 5/7 Safe 1.4.1)
#     - 5/7 Safe holds PROPOSER + EXECUTOR + CANCELLER on Timelock
#     - tETH vault owner() = Timelock; tETH token owner() = Timelock
#     - Rescuer contract has hardcoded 5-day delay + hardcoded recipient = 5/7 Safe
#   Per v3.9 multisig-threshold-floor: 5/7 = 71% ratio (>= 60% clause a) AND
#   5 absolute (>= 4 clause b) + 5-day timelock — passes both clauses cleanly.
#   The 2/5 Safe at 0xCBCC15... cited as "Treehouse pattern" is a DAO TREASURY
#   (holds TREE/USDC/NFTs) with NO role on Timelock or tETH contracts.
#   Score = 4 (not 3): signer identities undisclosed; core contracts upgradeable.
#   v4.3 Tier B+ NOT applicable (DeFi protocol, not regulated public issuer).
# - C4 = 2.90 (was 2.60 in v3.1): C4a unchanged at 2 (zero exploits, $250K bounty);
#   C4b 4 → 5 (operational age 18 → 20.4 months, low end of 1-2y band).
#   C4 = 0.7*2 + 0.3*5 = 2.90. v3.5 max(C4a, C4) = 2.90 < 8 (no cap).
# - C5 = 5 unchanged: Chainlink CCIP (compliant per v3.6 whitelist); 11 audited
#   external dependencies; off-chain keeper criticality offset by on-chain PPP
#   automatic unwind. No Part A elevation.
# - C6 = 2 unchanged: 3 doxxed co-founders (Brandon Goh ex-Morgan Stanley fixed
#   income); MassMutual Ventures + Lightspeed + Binance Labs institutional backing.
# Hard caps: NONE triggered.
# Verdict: APPROVED, PRS 5.08 → §5 Rule 2 cap 30%. Operational ceiling: 10% pending
# TVL recovery > $250M OR formal resolution of ToB Aug 2024 High findings.
